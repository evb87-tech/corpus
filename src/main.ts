// Entry point — wires features into a CLI / runtime.
// Keep this file thin: it imports composition roots from each feature and the shared layer.
// Domain & application logic NEVER live here.
export {};

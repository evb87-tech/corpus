// Loaded by bun test (see bunfig.toml [test] preload).
// Use this file for global test setup: env, mocks, fakes that every test layer needs.
// Keep it minimal — feature-specific fixtures live next to the feature.
export {};
